rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // WARNING: This is insecure and for debugging purposes only.
    // It allows anyone to read from and write to your storage bucket.
    match /{allPaths=**} {
      allow read, write: if true;
    }
  }
}
